defmodule Wimf.SMSView do
  use Wimf.Web, :view

  @attributes ~W(message)

  # def render("response.json", %{data: data}) do
  #   data
  # end

  #def render("response.json", %{data: data}) do
    #twiml do
    #  sms "What up! -From Elixir"
    #end
  #end
end
