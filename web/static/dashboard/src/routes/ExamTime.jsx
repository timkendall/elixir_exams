export default class User extends React.Component {
  render() {
    let { examTimeID } = this.props.params
    let { query } = this.props.location

    return (
      <div className="ExamTime">
        <h1>Exam time id: {userID}</h1>
        {age}
      </div>
    )
  }
}
